<!DOCTYPE html>
<html lang="en">
<head>
    <% include partials/head %>
</head>
<body class="container">

<header>
    <% include partials/header %>
</header>

<main>
    <h3><%= gw_title %></h3>
    <div class="row">
        <div class="col-sm-6">
            <div class="card">
                <div class="card-header">
                    <i class="far fa-money-bill-alt"></i>
                    Currency: <%= CURRENCY %>
                </div>
                <div class="card-body">
                    <form action="/api/v1/sendFiatVia/<%= gw_id %>" method="post" >
                        <div class="form-group">
                            <label for="amount">Amount (to deposit): </label>
                            <input type="number" required class="form-control" id="amount" name="amount"
                                   placeholder="Enter amount">
                            <small id="emailHelp" class="form-text text-muted">
                                This amount must match with your deposit.
                            </small>
                        </div>
                        <div class="form-group">
                            <label for="amount">Code/Id: </label>
                            <input type="text" class="form-control" id="code" name="code"
                                   placeholder="Identification" value="<%= EMAIL %>">
                            <small id="emailHelp" class="form-text text-muted">
                                Input here any code or information requested by gateway to process your
                                payment. Normaly, gateways can confirm you request on bank, online service by your ID, email
                                or any account number. Ex: on paypal gateway can confirm by your account name or email.
                                From banck: you need transaction id or account ID.
                            </small>
                        </div>
                        <div class="form-group form-check">
                            <input type="checkbox" required class="form-check-input" id="terms">
                            <label class="form-check-label" for="terms"> Terms and Conditions</label>
                        </div>

                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-chevron-circle-right"></i>
                            Proceed with deposit
                        </button>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="card">
                <div class="card-header">
                    <i class="far fa-money-bill-alt"></i>
                    Terms and Conditions
                </div>
                <div class="card-body">
                    <h5 class="card-title">Location: <%= gw_location %>, Fees: <%= fee %>%</h5>
                    <%- gw_terms %>
                </div>
            </div>
        </div>
    </div>
</main>

<footer>
    <% include partials/footer %>
</footer>

</body>
</html>
